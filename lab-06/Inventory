---
- hosts: localhost
  connection: local
  gather_facts: yes
  tasks:
    - name: Gather EC2 remote facts.
      ec2_remote_facts:
        region: "{{ region }}"
        filters:
          instance-state-name: running
      register: ec2_remote_facts
